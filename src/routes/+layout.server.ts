// Prerenders pages with auto-discovery or explicitly through the `entries()` function.
// Also renders pages on-demand when not pre-rendered.
// See https://svelte.dev/docs/kit/page-options#prerender
export const prerender = 'auto';
